<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Recognition and on-device sound detection both capture audio. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Everyday and Professional need the network; monitoring does not. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Environmental monitoring keeps listening with the app in the
         background, which Android 14+ requires a typed foreground service for. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- The monitoring notification, and Android 13+ runtime permission. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Alerts are multi-channel because the user cannot rely on sound. -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.FLASHLIGHT"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Monitoring can be restored after a restart when the user left it on. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- The torch alert is optional: a device without one still gets every
         other channel, so the app must remain installable. -->
    <uses-feature android:name="android.hardware.camera.flash" android:required="false"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="true"/>

    <application
        android:label="HumSukhan"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Password-recovery links come back into the app. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="pk.humsukhan" android:host="auth-callback"/>
            </intent-filter>
        </activity>

        <!-- Environmental monitoring. The type must be declared here *and*
             granted by FOREGROUND_SERVICE_MICROPHONE above, or the service
             throws on Android 14+. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="microphone"
            android:exported="false"
            android:stopWithTask="false"/>

        <!-- One-tap monitoring from the notification shade. -->
        <service
            android:name=".MonitoringTileService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_monochrome"
            android:label="@string/monitoring_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="false"/>
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Android 11+ package visibility. A service the app resolves at runtime
         and does not declare here is invisible to it: the recogniser entry is
         missing from most manifests and that is exactly how the device
         recogniser became undiscoverable (docs/instructions.md §5.1 B14). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
